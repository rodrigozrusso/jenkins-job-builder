- job-template:
    name: workflow_003-{name}
    project-type: workflow
    script: |
        node {{
            echo 'hello from Workflow'
        }}

- project:
    name: hello
    jobs:
        - 'workflow_003-{name}'
